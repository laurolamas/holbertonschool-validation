name: module3_task1

on:
  push:
    branches:
      - main
  schedule:
    - cron: "0 0 * * *"

jobs:
  module3_task1:
    runs-on: ubuntu-22.04

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Run Setup
        run: cd module3_task0 && ./setup.sh

      - name: Make Build
        run: cd module3_task0 && make build
